// Usando Map
const array [1, 2, 3, 4, 5];

array.map((item) => item * 2); // retorno: [2, 4, 6, 8, 10] (vai existir os 2 arrays)


// Usando forEach
const array [1, 2, 3, 4, 5];

array.forEach((item) => item * 2); // retorno : undefined (Requer uma constante para pegar o valor)


/* O que muda:
- Valor de retorno
- Considere se o array auxiliar será necessário
*/